---
ghes_upgrade_external_url: https://github.example.com

# Replica hosts intentionally do not define upgrade topology or approval values.
# Do not target them with playbooks/upgrade-ha-primary.yml.
